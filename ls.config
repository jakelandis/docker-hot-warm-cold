input {
  generator {
   threads => 10 
   message => "test"
   count => 100
  }
}

output {
  elasticsearch {
     hosts => ['elasticsearch-hot:9200'] 
     ilm_enabled => "false"
     index => 'test'
     action => create
  }
  stdout { codec => dots }
}
